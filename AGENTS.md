# Agent Instructions

Before editing this project:

1. Read `refs/project.yaml`.
2. Read `docs/prd/worldbuilder-map-generator-prd.md`.
3. Read `refs/planning/decisions.yaml`, `refs/planning/roadmap.yaml`, and `refs/planning/todos.yaml`.
4. Read `refs/architecture/overview.md` before changing module boundaries.
5. Read `refs/testing/validationCommands.yaml` before finalizing.

Keep durable decisions and handoff context in `refs/`. Do not store secrets in this repository.
