# Backup And Sync

Back up source, docs, refs, schema files, and small deterministic test fixtures.

Do not commit generated export folders, build output, caches, secrets, or machine-local credentials. Large generated maps should be regenerated from seed/config unless explicitly promoted to a small golden fixture.
