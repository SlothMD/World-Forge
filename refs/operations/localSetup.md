# Local Setup

Application code has not been scaffolded yet.

Current validation:

```powershell
python refs/tools/validate_refs.py --mode initialized
```

After TypeScript scaffolding exists, add install, typecheck, test, and dev-server commands here.
