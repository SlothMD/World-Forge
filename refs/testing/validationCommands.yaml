version: 1
schema: refs/schemas/schemaRegistry.yaml
updated: 2026-06-24
commands:
  - id: validate-refs-initialized
    command: python refs/tools/validate_refs.py --mode initialized
    purpose: Validate the initialized project-memory harness.
  - id: future-typecheck
    command: npm run typecheck
    purpose: Run TypeScript type checks after package scaffolding exists.
    status: planned
  - id: future-tests
    command: npm test
    purpose: Run unit and golden seed tests after package scaffolding exists.
    status: planned
